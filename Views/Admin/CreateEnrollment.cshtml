@model Enrollment
@{
    ViewBag.Title = "Create Enrollment";
    Layout = "_AdminLayout";

    var students = ViewBag.Students as List<Student>;
    var courses = ViewBag.Courses as List<Course>;
}

<h1 class="text-center mb-4">Add New Enrollment</h1>

<form asp-action="CreateEnrollment" method="post" class="w-50 mx-auto">

    <!-- Student -->
    <div class="mb-3">
        <label class="form-label">Student</label>
        <select asp-for="StudentId" class="form-control" required>
            <option value="">Select Student</option>
            @foreach (var s in students)
            {
                <option value="@s.StudentId">@s.Name</option>
            }
        </select>
    </div>

    <!-- Course -->
    <div class="mb-3">
        <label class="form-label">Course</label>
        <select asp-for="CourseId" class="form-control" required>
            <option value="">Select Course</option>
            @foreach (var c in courses)
            {
                <option value="@c.CourseId">@c.CourseName</option>
            }
        </select>
    </div>

    <!-- Attendance Date -->
    <div class="mb-3">
        <label class="form-label">Attendance Date</label>
        <input asp-for="EnrollmentDate" type="date" class="form-control" />
    </div>

  

    <button type="submit" class="btn btn-primary w-100">
        Save Enrollment
    </button>

</form>
